filters:
  and:
    - file.path.contains("protected")
views:
  - type: table
    name: Table
    filters:
      and:
        - file.path.contains("deck")
    order:
      - file.name
      - publish
      - password
